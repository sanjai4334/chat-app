import "./ChatPage.css";

const ChatPageFallback = () => {
    return (
        <div className="chat-page fallback-page">
            Select a user and start chatting!
        </div>
    );
};

export default ChatPageFallback;
